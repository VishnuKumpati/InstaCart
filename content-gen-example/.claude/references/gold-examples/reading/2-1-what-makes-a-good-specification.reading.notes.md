# Why this passes — 2.1 reading (standard depth, conceptual topic)

**Provenance:** `origin/week-2` — team-reviewed content (ChatGPT review pass). Treat as the
quality bar for **standard-depth conceptual topics**.

Moves that make it beginner-clear:

- Defines **specification** in plain words in the very first sentence — no jargon before the definition.
- Recaps prior-week concepts (finite/definite algorithms, AI non-determinism) *before* building on them, in one short paragraph.
- Each of the four properties gets its own `###` heading with a **Good / Bad example pair** — concrete before abstract, every time.
- Paragraphs stay ≤4 sentences; lists carry the weight.
- Bold-term definitions on first use; no acronym appears unexpanded.
- Stays on the headline: teaches the four properties only; adjacent topics (testing a spec, iterating) are deferred to their own topics.
- ⚠️ **Do NOT replicate the A1 assessment tie-in.** This example (and 3-4) mentions "supports A1 —
  Specification Portfolio (due W3, 10%)". Team decision 2026-06-11: assessment references are LMS
  metadata, never learner prose. Builders must not copy this move; reading-critic flags it
  (`assessment_reference`).

Word count ≈ 1200. Markedly denser or wider output than this should be flagged.
