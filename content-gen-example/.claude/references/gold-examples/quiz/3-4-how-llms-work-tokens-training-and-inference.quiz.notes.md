# Why this passes — 3.4 quiz (GIFT)

**Provenance:** `origin/week-3` — shipped alongside the team-reviewed reading. Confirm quiz
review status with the content lead before treating as fully load-bearing.

Same structural bar as the 2.1 quiz example (scenario stems, per-option feedback, `####`
corpus-section rationale). Included as the **deep technical topic** counterpart: stems stay in
plain language even when testing technical content (tokens, prefill/decode, KV cache) — the
question difficulty comes from the concept, never from the wording.
